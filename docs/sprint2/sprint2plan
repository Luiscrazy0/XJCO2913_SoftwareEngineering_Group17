##Sprint 2 任务清单（中文）

### 一、核心功能实现（Priority 2，共15项）

| 序号 | 任务描述 | 对应ID |
|:---|:---|:---|
| 1 | 存储客户银行卡信息以便快速预订（需加密） | ID 2 |
| 2 | 用户账户安全保护（如加密存储、防暴力破解等） | ID 3 |
| 3 | 预订成功后发送邮件确认 | ID 7 |
| 4 | 员工为未注册用户代订 | ID 9 |
| 5 | 预订时将滑板车状态从“可租”更新为“不可租” | ID 10 |
| 6 | 提供当前预订的续租选项 | ID 11 |
| 7 | 用户提交问题/故障的简短反馈 | ID 13 |
| 8 | 展示滑板车列表及可用性/位置信息 | ID 17 |
| 9 | 在高德地图上展示五个滑板车取车点 | ID 18 |
| 10 | 按租赁类型（1小时/4小时/天/周）查看周收入 | ID 19 |
| 11 | 查看一周内的每日综合收入 | ID 20 |
| 12 | 将周收入数据绘制成图表 | ID 21 |
| 13 | 为高频用户（每周8+小时）、学生、老年人提供折扣 | ID 22 |
| 14 | 支持多客户端并发使用 | ID 23 |
| 15 | 响应式用户界面（适配不同设备） | ID 24 |
| 16 | 无障碍设计（颜色对比度、字体选择等） | ID 25 |

### 二、其他职责

| 序号 | 任务描述 |
|:---|:---|
| 17 | 参与代码审查，每个PR至少批准1人后方可合并 |
| 18 | 与梁加桐配合，确保功能完成后测试可及时跟进 |

---

## 额外建议

### 1. 优先级排序（建议按此顺序开发）

Sprint 2任务较多（16项），建议按以下优先级推进，确保核心流程先跑通：

| 优先级 | 任务 | 理由 |
|:---|:---|:---|
| **P0（必须先做）** | 滑板车状态更新（ID10）、多客户端并发（ID23） | 核心业务逻辑，影响演示 |
| **P1（高）** | 续租（ID11）、地图展示（ID18）、可用性展示（ID17） | 用户体验关键功能 |
| **P2（中）** | 周收入/图表（ID19,20,21）、折扣（ID22） | 管理员功能，可稍后 |
| **P3（低）** | 邮件确认（ID7）、银行卡存储（ID2）、员工代订（ID9） | 增强功能，可放到Sprint 2后半段 |
| **P4（最后）** | 响应式UI（ID24）、无障碍设计（ID25） | 前端优化，可最后收尾 |

### 2. 技术实现建议

**高德地图集成**
- 提前申请Web端API Key（免费额度足够）
- 建议使用高德地图JavaScript API
- 五个位置可以用模拟坐标（方便演示），后续可配置

**邮件确认**
- 可使用Nodemailer + 测试邮箱（如Ethereal）
- 或使用Resend/SendGrid免费额度
- 建议先实现异步发送，避免阻塞主流程

**并发处理**
- 确保数据库连接池配置正确（Prisma默认有连接池）
- 滑板车状态更新使用数据库事务 + 乐观锁

**折扣系统**
- 建议在用户表加字段：user_type（normal/student/senior）
- 折扣规则配置化，方便后续调整

### 3. 工作节奏建议

| 阶段 | 时间 | 目标 |
|:---|:---|:---|
| Week 1 | 3/17-3/21 | P0任务：滑板车状态、并发支持、基础API |
| Week 2 | 3/24-3/28 | P1任务：地图、可用性展示、续租 |
| Week 3 | 3/31-4/4 | P2任务：收入统计、折扣、邮件确认 |
| Week 4（缓冲） | 4/7-4/11 | P3/P4任务、bug修复、与测试团队联调 |

### 4. 风险提示

| 风险 | 建议 |
|:---|:---|
| 任务过多导致延期 | 与SM沟通，必要时将ID2（银行卡存储）移到Sprint 3 |
| 地图API不熟悉 | 提前一天做技术预研，先写demo验证 |
| 与测试配合脱节 | 每完成一个模块立即通知梁加桐，不要等到全部做完 |
| 代码审查耗时 | 提交PR时写清楚测试方法，减少审查来回 |

### 5. 与梁加桐的协作建议

梁加桐在Sprint 2需要做E2E测试、分支测试、覆盖率门禁等。建议你：

1. **提前提供测试接口**：每个模块完成后，输出简单的API文档或Postman集合
2. **保持分支整洁**：功能分支命名规范（如 `feature/booking-extension`），方便测试定位
3. **关注覆盖率报告**：CI加入覆盖率门禁后，注意查看哪些代码没覆盖，及时补充
---

有任何技术细节问题（如高德地图具体集成方式、Prisma事务处理等），随时在群里问！加油 💪
