# Sprint 1 - 阶段6：登出与Token管理

## 目标
实现完整的Token生命周期管理，包括登出功能、API请求自动携带Token、Token过期处理和认证守卫完善。

## 完成标准
1. 导航栏添加登出功能
2. 实现完整的登出流程
3. API请求自动携带Authorization头
4. Token过期或无效时自动处理
5. 完善ProtectedRoute认证守卫
6. 实现角色权限检查

## 核心任务

### 1. 实现登出功能
- 在导航栏添加登出按钮
- 实现登出逻辑：清除localStorage中的token和用户信息
- 更新AuthContext状态
- 登出后跳转到登录页面
- 显示登出成功提示

### 2. 完善Token生命周期管理
- 确保所有API请求自动携带Authorization头
- 实现请求拦截器添加token
- 实现响应拦截器处理401错误
- Token过期时自动跳转到登录页
- 清除过期的本地存储数据

### 3. 完善认证守卫（ProtectedRoute）
- 检查用户是否已认证（token是否存在）
- 检查用户角色权限（如需特定角色）
- 未认证用户重定向到登录页
- 角色不符用户显示无权限提示
- 保存原始访问路径，登录后重定向

### 4. 更新导航栏组件
- 根据认证状态显示不同内容
- 已登录用户显示用户邮箱和登出按钮
- 未登录用户显示登录/注册链接
- 根据用户角色显示不同的导航菜单
- 添加响应式导航菜单

### 5. 实现Token安全处理
- Token存储安全考虑
- 防止XSS攻击的存储方式
- 登出时彻底清除认证状态
- 页面刷新时恢复认证状态

## 功能实现细节

### 登出流程
1. 用户点击登出按钮
2. 调用authApi.logout()方法
3. 清除localStorage中的token和user数据
4. 更新AuthContext状态为未认证
5. 跳转到登录页面
6. 显示"已成功登出"提示信息

### API请求拦截器
```typescript
// 请求拦截器 - 添加token
apiClient.interceptors.request.use(config => {
  const token = localStorage.getItem('token');
  if (token) {
    config.headers.Authorization = `Bearer ${token}`;
  }
  return config;
});

// 响应拦截器 - 处理401错误
apiClient.interceptors.response.use(
  response => response,
  error => {
    if (error.response?.status === 401) {
      // Token过期或无效
      localStorage.removeItem('token');
      localStorage.removeItem('user');
      window.location.href = '/login';
    }
    return Promise.reject(error);
  }
);
```

### ProtectedRoute逻辑
1. 检查localStorage中是否有token
2. 检查AuthContext中的用户状态
3. 如果requiredRole参数存在，检查用户角色
4. 未认证用户：重定向到/login，保存原始路径
5. 角色不符用户：显示无权限页面
6. 认证通过用户：渲染子组件

### 导航栏状态管理
- **未登录状态**：
  - 显示：系统Logo
  - 显示：登录/注册链接
- **CUSTOMER登录状态**：
  - 显示：用户邮箱
  - 显示：车辆列表链接
  - 显示：我的预约链接
  - 显示：登出按钮
- **MANAGER登录状态**：
  - 显示：用户邮箱（管理员标识）
  - 显示：管理后台链接
  - 显示：登出按钮

## 安全考虑

### Token存储安全
- 使用localStorage存储token（简单实现）
- 考虑未来使用httpOnly cookies增强安全
- 避免在代码中硬编码token处理逻辑

### XSS防护
- 对用户输入进行适当转义
- 避免在DOM中直接插入用户数据
- 使用React的自动转义特性

### 会话管理
- 页面刷新时从localStorage恢复会话
- 提供"记住我"功能选项（增强功能）
- 实现会话超时自动登出（增强功能）

## 验证测试
1. 登录后导航栏显示用户信息和登出按钮
2. 点击登出按钮，验证成功登出并跳转
3. 测试API请求是否自动携带token
4. 模拟token过期，验证自动跳转到登录页
5. 未登录用户访问受保护页面被重定向
6. CUSTOMER用户尝试访问/admin页面被阻止
7. MANAGER用户可以正常访问/admin页面
8. 页面刷新后认证状态保持
9. 多标签页会话同步测试

## 用户体验优化
- 登出前的确认对话框（防止误操作）
- 登出成功提示信息
- 无权限访问的友好提示页面
- 登录后重定向到原始访问页面
- 加载状态的视觉反馈


下面是将你的配色系统**落到“Token管理 + 登出 + 权限控制相关UI（Navbar / Toast / 错误页）”的页面级设计规范**。这一阶段不再是单一页面，而是**跨页面的全局交互层设计**，重点是：

* 状态反馈（成功 / 过期 / 无权限）
* 全局一致性（所有页面共享）
* 不打断用户但又足够明确

可以直接加入 Sprint 文档。

---

# 🎨 Sprint 1 - 阶段6：认证状态与Token管理 UI 颜色设计规范

## 设计目标

该阶段 UI 的核心是“系统状态表达”，颜色需要做到：

* 清晰反馈认证状态变化（登录 / 登出 / 过期）
* 不干扰主流程（非侵入式提示）
* 强调安全与可信（稳定、克制）

---

## 1️⃣ Navbar（认证状态核心展示）

### 背景与结构

| 元素 | 颜色        |
| -- | --------- |
| 背景 | `#FFFFFF` |
| 边框 | `#E2E8F0` |
| 文本 | `#0F172A` |

---

### 登录状态显示

#### 用户信息

| 元素            | 颜色           |
| ------------- | ------------ |
| 邮箱            | `#334155`    |
| 角色标签（MANAGER） | 🔵 `#3B82F6` |

👉 示例：

```
user@email.com   [ADMIN]
```

---

### 导航链接

| 状态    | 颜色         |
| ----- | ---------- |
| 默认    | #334155    |
| Hover | #0F172A    |
| 当前页面  | 🟢 #22C55E |

---

### 登出按钮（重要）

| 状态    | 颜色           |
| ----- | ------------ |
| 默认    | 🔴 `#EF4444` |
| Hover | `#DC2626`    |
| 文字    | #FFFFFF      |

👉 设计原则：

> 登出是“破坏性操作” → 使用红色而不是绿色

---

## 2️⃣ 登出确认弹窗（可选但推荐）

### Modal样式

| 元素      | 颜色                 |
| ------- | ------------------ |
| Overlay | rgba(15,23,42,0.6) |
| 背景      | #FFFFFF            |

---

### 按钮

| 按钮             | 颜色         |
| -------------- | ---------- |
| Cancel         | #E2E8F0    |
| Confirm Logout | 🔴 #EF4444 |

---

## 3️⃣ 全局提示系统（Toast / Alert）

这是本阶段最关键 UI。

---

### 成功提示（Success Toast）

| 元素 | 颜色           |
| -- | ------------ |
| 背景 | `#DCFCE7`    |
| 图标 | 🟢 `#10B981` |
| 文字 | `#065F46`    |

👉 示例：

```
Logged out successfully
```

---

### 错误提示（Error Toast）

| 元素 | 颜色           |
| -- | ------------ |
| 背景 | `#FEE2E2`    |
| 图标 | 🔴 `#EF4444` |
| 文字 | `#7F1D1D`    |

👉 示例：

```
Session expired. Please login again.
```

---

### 信息提示（Info Toast）

| 元素 | 颜色           |
| -- | ------------ |
| 背景 | `#DBEAFE`    |
| 图标 | 🔵 `#3B82F6` |
| 文字 | `#1E3A8A`    |

---

## 4️⃣ Token过期处理 UI（关键）

### 自动跳转前提示

| 元素    | 颜色           |
| ----- | ------------ |
| 提示条背景 | 🔴 `#FEE2E2` |
| 文本    | `#7F1D1D`    |

👉 示例：

```
Your session has expired. Redirecting to login...
```

---

### 跳转后（登录页）

* 使用 Error Toast（红色）
* 不需要重复展示复杂信息

---

## 5️⃣ 无权限页面（403 Page）

### 页面颜色设计

| 元素 | 颜色         |
| -- | ---------- |
| 背景 | #F8FAFC    |
| 图标 | 🔴 #EF4444 |
| 标题 | #0F172A    |
| 描述 | #64748B    |

---

### 操作按钮

| 按钮        | 颜色         |
| --------- | ---------- |
| 返回首页      | 🟢 #22C55E |
| 联系管理员（可选） | 🔵 #3B82F6 |

---

## 6️⃣ ProtectedRoute加载状态

### 页面保护加载

| 元素      | 颜色         |
| ------- | ---------- |
| Spinner | 🔵 #3B82F6 |
| 背景      | #F8FAFC    |

👉 用于：

* 页面刷新恢复token
* 权限校验过程中

---

## 7️⃣ 多标签页同步提示（可选）

当用户在其他标签页登出：

| 元素   | 颜色         |
| ---- | ---------- |
| 提示背景 | 🟡 #FEF3C7 |
| 文字   | #92400E    |

👉 示例：

```
You have been logged out in another tab
```

---

## 8️⃣ Dark Mode（全局状态UI）

### Navbar

| 元素 | HEX     |
| -- | ------- |
| 背景 | #1E293B |
| 文字 | #F1F5F9 |

---

### Toast

| 类型 | HEX     |
| -- | ------- |
| 成功 | #22C55E |
| 错误 | #EF4444 |
| 信息 | #3B82F6 |

（背景降低透明度）

---

## 9️⃣ 全局视觉优先级（关键）

```
1️⃣ 错误状态（红色）→ Token失效 / 无权限
2️⃣ 成功状态（绿色）→ 登录 / 登出成功
3️⃣ 当前导航（绿色）
4️⃣ 普通信息（灰色）
```

---

## 🔟 前端实现建议（工程化）

### 新增全局 Token

```css
--toast-success-bg: #DCFCE7;
--toast-error-bg: #FEE2E2;
--toast-info-bg: #DBEAFE;

--logout-button: #EF4444;
--navbar-active: #22C55E;
```

---

### Toast组件结构

```tsx
<Toast type="success | error | info">
  Message
</Toast>
```

---

### 拦截器触发UI

```tsx
if (error.status === 401) {
  showToast('Session expired', 'error');
  logout();
}
```

---

## 总结（文档用）

本阶段的颜色设计从“页面级”上升到“系统级”，核心原则：

* **红色 → 安全/错误（最高优先级）**
* **绿色 → 成功/当前状态**
* **蓝色 → 信息/过程**
* **灰色 → 基础信息**

通过：

* Navbar状态展示
* Toast反馈系统
* 权限与异常页面设计

构建一个**完整的认证状态可视化体系**，保证用户在任何情况下都能理解系统当前状态。

---

## 下一步
完成本阶段后，进入阶段7：管理后台实现。